# Antipatterns

Approaches that failed or caused issues. Capture so we don't repeat them.

**Manual file edits made outside Claude Code mid-session can silently strip sections from context files. Always verify critical files like root CLAUDE.md are intact after any manual edit. Use Get-Content to confirm before and after.**
